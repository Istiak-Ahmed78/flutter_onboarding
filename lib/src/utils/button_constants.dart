import 'package:flutter/material.dart';

///Default Skip Button style
class DefaultSkipButtonStyle {
  static const Color defaultButtonColor = Colors.grey;
  static const Text defaultSkipText = Text('Skip');
  static const double defaultBorderRaduis = 20.0;
  static const EdgeInsets defaultPadding = EdgeInsets.all(6);
}

///Default Procced Button style
class DeafultPreccedButtonStrle {
  static const Color defaultButtonColor = Colors.grey;
  static const Text defaultProccedText = Text('Next');
  static const double defaultBorderRaduis = 20.0;
  static const EdgeInsets defaultPadding = EdgeInsets.all(6);
}
